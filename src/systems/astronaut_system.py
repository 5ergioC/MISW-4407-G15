class AstronautSystem:
    def update(self, world, dt: float) -> None:
        del world
        del dt
        # TODO(P3): Handle astronaut walking, captured, falling, carried, deposited, and dead states.
