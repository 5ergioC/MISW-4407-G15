class DebugSystem:
    def update(self, world, dt: float) -> None:
        del world
        del dt
        # TODO(P4-BONUS): Toggle debug state from config/debug.json.

    def render(self, world, surface) -> None:
        del world
        del surface
        # TODO(P4-BONUS): Draw colliders, entity ids, AI states, and camera bounds.

