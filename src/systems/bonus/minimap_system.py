class MinimapSystem:
    def render(self, world, surface, camera) -> None:
        del world
        del surface
        del camera
        # TODO(P4-BONUS): Render world-scale player, enemies, astronauts, and abduction alerts.

