class CollisionSystem:
    def update(self, world, dt: float) -> None:
        del world
        del dt
        # TODO(P2): Resolve laser, enemy projectile, astronaut rescue, score, audio, and particle collisions.
