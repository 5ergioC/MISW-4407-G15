class EnemySpawnSystem:
    def update(self, world, dt: float) -> None:
        del world
        del dt
        # TODO(P3): Spawn Landers, Mutants, and astronauts progressively from config/waves.json.
