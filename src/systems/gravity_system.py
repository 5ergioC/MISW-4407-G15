class GravitySystem:
    def update(self, world, dt: float) -> None:
        del world
        del dt
        # TODO(P3): Apply gravity to falling astronauts and kill them when fall height exceeds config.
