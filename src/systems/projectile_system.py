class ProjectileSystem:
    def update(self, world, dt: float) -> None:
        del world
        del dt
        # TODO(P3): Move enemy bullets/missiles, expire them, and support missile-specific behavior.
