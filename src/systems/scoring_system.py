class ScoringSystem:
    def update(self, world, shared_state: dict[str, object]) -> None:
        del world
        del shared_state
        # TODO(P2): Consume score events/components and update shared_state["score"].

